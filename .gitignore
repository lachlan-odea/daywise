node_modules
dist
dist-ssr
*.local

# Local environment files (keep .env.example tracked)
.env
.env.*
!.env.example

# Firebase service-account keys. These grant full admin access and BYPASS Firestore
# security rules entirely — never commit one. This repo is public.
serviceAccount*.json
*-firebase-adminsdk-*.json
firebase-adminsdk*.json
.DS_Store
.vscode/*
!.vscode/extensions.json

# Local Claude Code settings
.claude/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
